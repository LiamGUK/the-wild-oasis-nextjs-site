// To create nested routes in Next.js simply create a new folder within a route folder and add another page.js file inside
// React component will be rendered under a route linked to parent route - this case /cabins/test

export default function Page() {
  return <h1>Nested route of Cabins page</h1>;
}
